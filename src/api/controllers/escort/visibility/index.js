const { createVisibility } = require("./visibilityController");
const { getVisibility } = require("./getVisibility");
const { checkVisibility } = require("./checkVisibility");

module.exports = { createVisibility, getVisibility, checkVisibility };
